# User-specific variables
# WARNING:    Due to the sensitive nature of this file, make sure it is not tracked by git
# WARNING:    You can tell git to assume it is unchanged by running:
# WARNING:        git update-index --assume-unchanged py/user_vars.py 
# WARNING:    You can revert this (to update the file) with
# WARNING:        git update-index --no-assume-unchanged py/user_vars.py 
class UserVars():
    # Fill this with your Discord bot's API key
    DISCORD_API = ''
    # Fill this with {name}#{discriminator} (e.g. bigman#1234)
    DISCORD_USER_ID = ''
    # ID of channel the bot should respond to
    DISCORD_BOT_MSG_CHANNEL = ''
